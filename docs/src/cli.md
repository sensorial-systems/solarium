# CLI Reference

Install:

```bash
cargo install solarium-cli
```

Commands:

```bash
solarium build      # Build the workspace
solarium test       # Run tests
solarium test --detach  # Run tests and keep the validator running afterward
solarium dev        # Start local validator and rebuild on changes
solarium deploy     # Deploy the workspace
```
